import { PageHeader, EmptyState } from '../components/ui';

export function CampaignsPage() {
  return (
    <>
      <PageHeader title="Campaigns" />
      <EmptyState title="Campaigns view is being built">The data is loaded; this screen is next.</EmptyState>
    </>
  );
}
