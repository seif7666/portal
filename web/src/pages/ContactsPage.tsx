import { PageHeader, EmptyState } from '../components/ui';

export function ContactsPage() {
  return (
    <>
      <PageHeader title="Contacts" />
      <EmptyState title="Contacts view is being built">The data is loaded; this screen is next.</EmptyState>
    </>
  );
}
